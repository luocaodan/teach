- content_for :scripts do
  = javascript_pack_tag 'burndown'
- content_for :styles do
  = stylesheet_pack_tag 'burndown'

#burndown-app{data: issues_data, 'v-loading': 'loading', 'element-loading-text': '加载中'}
  %v-chart{':options': 'burnOption'}

:css
  .echarts {
    width: 60%;
    margin: auto;
    min-width: 400px;
  }

