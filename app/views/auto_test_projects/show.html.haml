- content_for :scripts do
  = javascript_pack_tag 'test_records'
- content_for :styles do
  = stylesheet_pack_tag 'test_records'

#test-records{data: {records: student_test_records, endpoint: classroom_auto_test_project_path}, ':style': "{height: height + 'px'}", 'v-loading': 'loading', 'element-loading-text': '加载中'}
  %test-records


:css
  #test-records {
    overflow: auto;
  }